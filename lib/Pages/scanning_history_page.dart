import 'package:flutter/material.dart';

class ScanningHistoryPage extends StatelessWidget {
  const ScanningHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("This is the scanning histrory screen"),
      ),
    );
  }
}
